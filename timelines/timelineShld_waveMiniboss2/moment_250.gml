if (instance_number(obj_enemy_SH1_fast) >= 4 || instance_number(obj_enemy_parent) > 19) exit;
var spawn1 = instance_create_layer(xx + 1152, yy - 1152, "Enemy", obj_enemy_spawn);
spawn1.enemy = obj_enemy_SH1_faster;
if (instance_number(obj_enemy_SH1_fast) >= 3 || instance_number(obj_enemy_parent) > 14) exit;
var spawn2 = instance_create_layer(xx - 1152, yy + 1152, "Enemy", obj_enemy_spawn);
spawn2.enemy = obj_enemy_SH1_faster;
if (instance_number(obj_enemy_SH1_fast) >= 2 || instance_number(obj_enemy_parent) > 9) exit;
var spawn3 = instance_create_layer(xx + 1152, yy + 1152, "Enemy", obj_enemy_spawn);
spawn3.enemy = obj_enemy_SH1_faster;
if (instance_number(obj_enemy_SH1_fast) >= 1 || instance_number(obj_enemy_parent) > 4) exit;
var spawn4 = instance_create_layer(xx - 1152, yy - 1152, "Enemy", obj_enemy_spawn);
spawn4.enemy = obj_enemy_SH1_faster;