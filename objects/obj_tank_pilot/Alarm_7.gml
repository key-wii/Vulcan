//x_adjust = lengthdir_x(11, direction);
//y_adjust = lengthdir_y(11, direction);
if (weapon == 0) can_shoot = (y_gun_reload * shoot_speed) + 4;
if (weapon == 1) can_shoot = (g_gun_reload * shoot_speed) + 10;
if (weapon == 2) can_shoot = (b_gun_reload * shoot_speed) + 15;