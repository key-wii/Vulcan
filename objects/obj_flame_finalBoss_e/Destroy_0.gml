/*if (instance_exists(particleMachine))
	with (particleMachine) instance_destroy();*/
if (instance_exists(particleMachine2))
	with (particleMachine2) owner = id;
if (instance_exists(particleMachine3))
	with (particleMachine3) owner = id;