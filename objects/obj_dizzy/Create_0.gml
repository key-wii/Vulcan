event_inherited();
//particleMachine = instance_create_layer(x, y, "Player", obj_shell_particles);
//particleMachine.owner = id;

disappear = false;

image_speed = 6;
alarm_set(10, 1);
alarm_set(7, 60);